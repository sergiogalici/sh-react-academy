import styled from 'styled-components'

export type StyledBSTAProps = {
  mainTitle: string
  secondTitle: string
  buttonTitle: string
}

export const StyledBSInfoBox = styled.div`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
`
